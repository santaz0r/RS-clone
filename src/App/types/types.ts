type TSpec = {
  [key: string]: string | number;
};

type TDoc = {
  name: string;
  password: string;
  specialization: string;
  mail: string;
  username: string;
  image: string;
  surname: string;
  _id: string;
};

export type { TSpec, TDoc };
